name: Collect opportunities
on:
  schedule:
    - cron: '23 12 * * *'
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: opportunity-collection
  cancel-in-progress: false
jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.repository.default_branch }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: python -m pip install -r requirements.txt
      - name: Restore employer page cache
        uses: actions/cache@v4
        with:
          path: output/page-cache.json
          key: opportunity-pages-${{ runner.os }}-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: opportunity-pages-${{ runner.os }}-
      - name: Collect, screen, and prepare email
        id: collect
        run: python -m opportunities run
      - name: Upload workbook, screening report, and email draft
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: opportunity-review-${{ github.run_number }}
          path: |
            output/opportunities.xlsx
            output/opportunities.csv
            output/collection-report.json
            output/screening-report.json
            output/screening-report.html
            output/digest/
            OPPORTUNITIES.md
          if-no-files-found: warn
          retention-days: 30
      - name: Save listing history
        if: always() && (steps.collect.outcome == 'success' || steps.collect.outcome == 'failure')
        run: |
          if [ ! -f data/opportunities.json ]; then exit 0; fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/opportunities.json OPPORTUNITIES.md
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Refresh opportunity listings'
          git push
